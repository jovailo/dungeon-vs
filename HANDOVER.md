# DUNGEON VS - Claude Code 引き継ぎメモ

## プロジェクト概要

ローグライク × タワーディフェンスの非対称リアルタイムオンライン対戦ゲーム。
- **1P（勇者）**：ダンジョンを探索してB5Fのボスを倒す
- **2P（魔王）**：モンスター・罠を配置して勇者の攻略を阻止する
- WebSocket でリアルタイム通信、ターン制で進行

---

## リポジトリ・デプロイ情報

| 項目 | 内容 |
|---|---|
| GitHubリポジトリ | `https://github.com/[ユーザー名]/dungeon-vs` |
| 公開URL | `https://dungeon-vs-xxxx.onrender.com` |
| ホスティング | Render（無料プラン・15分無操作でスリープ） |
| Node.js バージョン | v20.10.0 |

---

## ファイル構成

```
dungeon-vs/
  ├── server.js        # WebSocketサーバー（Express + ws）
  ├── package.json     # 依存関係（express, ws）
  └── public/
       └── index.html  # ゲーム本体（フロントエンド全て1ファイル）
```

### server.js の役割
- Express で `public/` フォルダを静的配信
- WebSocket でルーム管理・メッセージ転送
- 公開ルーム一覧を全クライアントにブロードキャスト
- ポート: `process.env.PORT || 3000`（Render対応済み）

### index.html の構成（1469行）
- CSS（ロビー・ゲームUI・アニメーション）
- ロビー画面（ロール選択 → 公開/プライベートマッチング）
- ゲーム画面（Canvas描画 + サイドパネル）
- WebSocket通信処理
- ゲームロジック全体

---

## WebSocket 通信プロトコル

### クライアント → サーバー

| type | 内容 | 主なフィールド |
|---|---|---|
| `create_room` | ルーム作成 | `role`, `public`, `hostName` |
| `join_room` | ルーム参加 | `roomId`, `role` |
| `state_sync` | 状態同期 | `state`（ゲーム全体） |
| `demon_action` | 魔王の行動後 | `state` |
| `chat` | チャット | `text` |

### サーバー → クライアント

| type | 内容 | 主なフィールド |
|---|---|---|
| `room_created` | ルーム作成完了 | `roomId`, `role`, `public` |
| `game_start` | ゲーム開始 | `yourRole` |
| `state_sync` | 状態同期 | `state`, `endTitle`, `endMsg`, `endColor` |
| `demon_action` | 魔王行動の転送 | `state` |
| `public_rooms` | 公開ルーム一覧 | `rooms[]` |
| `chat` | チャット転送 | `role`, `text` |
| `opponent_disconnected` | 相手切断通知 | - |
| `error` | エラー | `message` |

### 状態同期の方針
勇者側がゲームの**正（マスター）状態**を持ち、行動するたびに `syncState()` で魔王へ全状態を送信。魔王側は受け取った状態を `applyStateSync()` で反映するだけ。

---

## ゲームロジック

### 定数・設定

```js
const COLS = 21, ROWS = 15, TS = 20; // マップサイズ・タイルサイズ(px)
const MAX_FLOOR = 5;                  // 勇者の勝利条件フロア

// 魔王ポイント（案A：配置制限 ＋ 案B：再生型）
function demonPtCapForFloor(f)  { return Math.min(8 + (f-1)*2, 20); } // 保有上限 8/10/12/14/16
function demonPtStartForFloor(f){ return Math.min(5 + (f-1)*2, 16); } // 突入時   5/7/9/11/13
const DEMON_REGEN_INTERVAL = 5;       // 5ターンごとに
const DEMON_REGEN_AMOUNT   = 1;       // 1pt回復（無限配置を防ぐため細く長く）
const DEMON_ACTIONS_PER_TURN = 2;     // 1ターンに配置できるカード枚数
```

### フロアごとの出現モンスター

```js
const FLOOR_ENEMY_TABLE = {
  1: ['slime'],
  2: ['slime','goblin'],
  3: ['goblin','skeleton'],
  4: ['skeleton','troll'],
  5: ['troll','dragon'],
};
```

### 勇者スキル（Q/E/R/Fキー）

| id | 名前 | 効果 | CD |
|---|---|---|---|
| slash | 斬撃 | 周囲8マス攻撃 | 5 |
| barrier | 障壁 | 2T無敵 | 7 |
| dash | 突進 | 2マス前進+強打 | 5 |
| sense | 見切り | 3T罠・敵の位置が見える | 8 |

### 魔王スキル（Z/X/C/V/A/S/D/Fキー）

| id | 名前 | 効果 | CD |
|---|---|---|---|
| quake | 地震 | 全敵ATK+2（2T） | 9 |
| curse | 呪い | 勇者ATK-1（3T） | 10 |
| revive | 復活 | 倒された敵を1体復活 | 10 |
| veil | 隠蔽 | 罠を全て未発動に戻す | 13 |
| darkness | 暗闇 | 勇者の視野を2T狭める | 12 |
| seal | 封鎖 | 階段を2T封印 | 13 |
| plunder | 略奪 | 勇者のGOLDを1/3奪う | 10 |
| magnet | 引き寄せ | 遠くの敵を勇者付近に集める | 12 |

### 魔王の操作キー

| キー | 動作 |
|---|---|
| 1〜7 | カード選択（現在のタブに応じる） |
| M | タブ切替（モンスター→特殊→罠） |
| Enter | 選択中のカードを召喚／設置 |
| **Space** | **待機（カード選択の有無に関わらずターンを渡す）** |
| Escape | カード選択の解除 |
| Z/X/C/V/A/S/D | 魔王スキル1〜7 |

### 魔王カード（数字キー1〜7 + Mキーでタブ切替）

**通常モンスター（フロア制限あり）**

| id | 名前 | HP | ATK | DEF | コスト |
|---|---|---|---|---|---|
| slime | スライム | 6+F*2 | 2+F | 0 | 1 |
| goblin | ゴブリン | 12+F*2 | 4+F | 1 | 3 |
| skeleton | スケルトン | 18+F*2 | 5+F | 2 | 5 |
| troll | トロル | 30+F*2 | 7+F | 3 | 7 |
| dragon | ドラゴン | 50+F*2 | 10+F | 4 | 9 |

**特殊モンスター（全フロア使用可）**

| id | 名前 | 特徴 | コスト |
|---|---|---|---|
| zombie | ゾンビ | 倒すと2体の欠片に分裂 | 4 |
| beehive | 蜂の巣 | 動かない・隣接でATK-1の毒 | 3 |
| bombrock | 爆弾岩 | 隣接で爆発HP-10・自壊 | 3 |

**罠カード（全フロア使用可）**

| id | 名前 | 効果 | コスト |
|---|---|---|---|
| dmgtrap | ダメージ罠 | HP-10 | 2 |
| warptrap | ワープ罠 | ランダム転送 | 2 |
| weaktrap | 弱体罠 | ATK-2・3T | 3 |
| slowtrap | 足止め罠 | 次Tスキップ | 2 |
| mimic | ミミック | アイテムに擬態・触れると戦闘 | 4 |
| bombtrap | 爆弾罠 | 隣接で範囲爆発HP-15 | 3 |
| poisontrap | 毒霧罠 | 毎ターンHP-1（5T） | 2 |

### 勇者の状態変数（hero オブジェクト）

```js
hero = {
  hp, maxHp,        // HP
  atk, def,         // 攻撃・防御
  lv, exp, gold,    // レベル・経験値・所持金
  barrier,          // バリア残りターン数
  atk_debuff,       // ATK低下量
  _weakTimer,       // 弱体残りターン数
  skip,             // 足止め残りターン数
  hunger,           // 満腹度（0〜100）
  maxHunger,        // 満腹度上限（100固定）
  darkness,         // 暗闇残りターン数
  sealedStairs,     // 階段封鎖残りターン数
  sense,            // 見切り残りターン数
  rev,              // 探索済みマス Array[ROWS][COLS]
  skills,           // HERO_SKILLSのコピー（CD管理）
  x, y,             // 現在位置
}
```

### ターン進行フロー

```
勇者が行動（移動/攻撃/スキル）
  ↓
endHeroTurn() が呼ばれる
  ↓
満腹度消費・自然回復・状態異常タイマー処理
  ↓
enemyAI() 実行（BFS経路探索で勇者を追う）
  ↓
demonPtLeft === 0 ? 自動スキップ : 魔王ターンへ
  ↓ 魔王ターン
魔王がカード召喚 or スキル発動 or 待機（Enter）
  ↓
endDemonTurn() → 勇者ターンへ
```

### 魔王ポイントの回復タイミング（案B：再生型）
- **フロア突入時**に `demonPtStartForFloor(f)` を付与（そのフロアの主な予算）
- **5ターンごとに +1pt** 再生（`demonPtCapForFloor(f)` が保有上限）
  - 毎ターン回復にすると敵を無限に配置できてしまうため、あえて細く長い補充にしている
- 上限まで貯めて高コストカードを出すか、小刻みに出すかの判断が生まれる
- PT0のとき：自動スキップ、スキルのみ割り込み発動可能

### 魔王の配置制限（案A）
- 1ターンに配置できるカードは **`DEMON_ACTIONS_PER_TURN`（=2）枚まで**
- 2枚配置すると自動でターン終了。**余ったポイントは次ターンへ持ち越し**
- これにより「一気に大量召喚」で勇者が詰む展開を防ぐ
- 状態変数は `demonActionsLeft`（魔王ターン開始時・階段降下時にリセット）

### 勝利条件
- **勇者の勝利**：B5Fで「魔王の使徒」（ボス）を倒す
- **魔王の勝利**：勇者のHPを0にする（戦闘・罠・飢えによる）

---

## 敵AI（BFS経路探索）

`bfsNextStep(ex, ey, tx, ty, m, others)` 関数で実装。
- 幅優先探索（BFS）で最短経路を計算
- 壁・他の敵を迂回して必ず追いかけてくる
- 視野10マス超の敵はスキップ（軽量化）
- ボスは引き寄せスキルで移動しない

---

## 満腹度システム

- 初期値：100
- 消費：2ターンに1減る（`turnCount % 2 === 0` のとき -1）
- 自然回復：満腹度30以上のとき5ターンごとにHP+1
- 飢え：満腹度0のとき3ターンごとにHP-2
- 食料アイテム：食料（+30）、大きなおにぎり（+50）、聖水（状態異常解除）

---

## ロビー画面の構成

```
ロール選択（勇者 or 魔王）
  ↓
タブ切替
  ├─ 公開マッチング
  │   ├─ 名前入力 → 公開ルーム作成
  │   └─ 待機中ルーム一覧（リアルタイム更新）→ クリックで参加
  └─ プライベート
      ├─ ルーム作成 → 6文字IDを相手に伝える
      └─ ID入力 → 参加
```

---

## 既知の課題・今後の改善候補

### バランス調整
- B5Fのボスが強すぎる/弱すぎる場合はHP・ATK・DEFを調整
- 魔王スキルのCDは現在かなり長め。対戦してみてさらに調整推奨

### 機能追加候補
- セーブ機能（途中でブラウザを閉じても再開できる）
- 観戦モード（3人目が両方の画面を見られる）
- ターンタイマー（制限時間制）
- リプレイ機能
- スコアランキング
- BGM・SE

### 技術的な課題
- Renderの無料プランは**15分無操作でスリープ**→有料プランまたは別サービスへの移行検討
- 状態をまるごと送る方式のため、通信量が多い。差分送信への最適化余地あり
- `index.html` が1ファイル1469行と大きい。将来的にはコンポーネント分割を検討

---

## ローカル開発環境の起動方法

```bash
cd dungeon-vs
npm install
node server.js
# → http://localhost:3000 でアクセス
```

## Renderへのデプロイ

```bash
git add .
git commit -m "変更内容"
git push
# → GitHubへのpushでRenderが自動デプロイ
```

---

## 開発経緯メモ

このゲームはClaude.aiとの対話を通じて段階的に開発されました。

1. ローカルHTMLのローグライク（ドット絵風）
2. ターン制2人対戦（同一画面）
3. 非対称対戦（勇者vs魔王）の設計
4. WebSocketによるWiFi内オンライン対戦
5. スキル・罠・ボス・満腹度システムの追加
6. BFS経路探索AIへの改善
7. Renderへのデプロイ（wss://対応）
8. 公開マッチング機能の追加
